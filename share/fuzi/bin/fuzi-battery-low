#!/bin/bash

set -euo pipefail

if (($# != 1)); then
  echo "Usage: fuzi-battery-low <percentage>" >&2
  exit 1
fi

level=$1

fuzi-notification-send -g 󱐋 -u critical "Time to recharge!" "Battery is down to ${level}%" -i battery-caution -t 30000
fuzi-hook battery-low "$level"
